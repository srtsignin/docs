# docs

## Serving the docs locally

```bash
git clone {this repo}
cd {this repo}
virtualenv venv
source venv/bin/activate
pip install -r requirements.txt
mkdocs serve

Optionally:
deactivate
```